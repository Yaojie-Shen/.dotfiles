# Bash-it
source "${HOME}/.shell/bash-it.bashrc"

# Devbox
source "${HOME}/.shell/devbox.zshrc"

# Aliases
source "${HOME}/.shell/aliases.sh"

# Exports
source "${HOME}/.shell/exports.sh"

# Functions
source "${HOME}/.shell/functions.sh"

# Custom
source "${HOME}/.shell/custom.sh"
