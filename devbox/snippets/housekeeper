#!/usr/bin/env bash
# Thin wrapper to run the Python module

if command -v readlink >/dev/null 2>&1; then
  SCRIPT_PATH=$(readlink -f "$0" 2>/dev/null || echo "$0")
else
  SCRIPT_PATH="$0"
fi
DIR=$(cd "$(dirname "$SCRIPT_PATH")" && pwd)
exec python3 "$DIR/housekeeper.py" "$@"
